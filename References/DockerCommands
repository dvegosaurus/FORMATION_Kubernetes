-- docker :
    - list
    - run (pull + start)
    - inspect
    - exec (run a comand in a container)
    - rm (delete in kubectl)

-- kubeclt :
    - get
    - run
    - describe
    - delete (rm in docker)
